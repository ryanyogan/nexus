---
title: Getting Started
description: Set up Nexus with your AI coding assistant in minutes
---

import { Tabs, TabItem, Card, CardGrid } from '@astrojs/starlight/components';

## Installation

Nexus works with any AI coding assistant that supports the Model Context Protocol (MCP). Below are setup instructions for popular clients.

### Claude Desktop

Add Nexus to your Claude Desktop configuration file:

- **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

```json title="claude_desktop_config.json"
{
  "mcpServers": {
    "nexus": {
      "url": "https://api.nexus.yogan.dev/mcp"
    }
  }
}
```

:::note
After saving the config, restart Claude Desktop for the changes to take effect.
:::

### Cursor

Add Nexus to your Cursor MCP configuration:

```json title=".cursor/mcp.json"
{
  "mcpServers": {
    "nexus": {
      "url": "https://api.nexus.yogan.dev/mcp"
    }
  }
}
```

### Other MCP Clients

For other MCP-compatible clients (Windsurf, Continue, etc.), add the Nexus server URL to your MCP configuration:

```
Server URL: https://api.nexus.yogan.dev/mcp
Protocol: MCP over HTTP
```

## Your First Query

Once configured, you can ask your AI assistant to use Nexus for documentation queries. Here's an example conversation:

> **You:** How do I create a route handler in Hono?

> **Assistant:** *(Uses resolve-library to find "hono", then query-docs to search)*
>
> In Hono, you can create route handlers using the HTTP method functions. Here's an example:

```typescript
import { Hono } from 'hono'

const app = new Hono()

app.get('/hello', (c) => {
  return c.text('Hello Hono!')
})

app.post('/users', async (c) => {
  const body = await c.req.json()
  return c.json({ user: body })
})
```

## Typical Workflow

The AI assistant typically follows this workflow when using Nexus:

1. **Resolve Library** - Find the library ID by name using `resolve-library`
2. **Query Documentation** - Search the library's docs using `query-docs`
3. **Provide Answer** - Use the retrieved documentation to give an accurate, up-to-date answer

:::tip[Automatic Tool Selection]
Most AI assistants will automatically determine when to use Nexus tools based on your question. You don't need to explicitly ask for documentation lookups.
:::

## Using Memory

Nexus's memory system lets your AI assistant remember context across sessions. No more re-explaining your project every time you start a new conversation.

### Save Project Context

At the start of a project (or when your architecture changes), ask your AI assistant to save the project context:

> **You:** Save this project context to Nexus: This is a pnpm monorepo with TanStack Start for the web app and Hono for the API. We use Cloudflare Workers for deployment, D1 for the database, R2 for storage, and Vectorize for embeddings. Auth is handled by Better Auth in the web app.

> **Assistant:** *(Uses save-memory with type "project_context")*
>
> I've saved the project context. In future sessions, I can recall this information automatically when you're working on this project.

:::note[Memory Types]
Use `project_context` for architecture and tech stack, `decision` for architectural decisions, `correction` for lessons learned, and `session_summary` for end-of-session notes.
:::

### Session Summaries

At the end of a coding session, save a summary of what was accomplished:

> **You:** Save a session summary: Today we implemented the memory system for Nexus. Added 6 new MCP tools (save-memory, recall-memories, get-project-context, list-memories, update-memory, delete-memory), created the D1 schema, set up Vectorize index, and integrated with the existing MCP server.

> **Assistant:** *(Uses save-memory with type "session_summary")*
>
> Session summary saved. Next time we work on this project, I'll be able to recall what we accomplished today.

### Recall Context

When starting a new session, your AI assistant can automatically recall relevant context:

> **You:** Let's continue working on Nexus.

> **Assistant:** *(Uses get-project-context or recall-memories)*
>
> I recall that Nexus is a pnpm monorepo with TanStack Start and Hono on Cloudflare Workers. In our last session, we implemented the memory system with 6 new MCP tools. What would you like to work on today?

:::tip[Automatic Context Retrieval]
You can ask your AI assistant to automatically retrieve project context at the start of each session. Many assistants will do this proactively when you mention continuing work on a known project.
:::

## Tips for Better Results

<CardGrid>
  <Card title="Be Specific" icon="pencil">
    Instead of "how do I use React hooks", ask "how do I use useEffect cleanup functions in React".
  </Card>
  <Card title="Mention the Library" icon="document">
    Include the library name in your question for faster resolution: "How do I create middleware in Hono?"
  </Card>
  <Card title="Ask for Examples" icon="open-book">
    Nexus indexes code examples, so asking "show me an example of..." often yields helpful snippets.
  </Card>
  <Card title="Save Important Decisions" icon="approve-check">
    When you make architectural decisions, ask your AI to save them. This prevents re-debating the same issues.
  </Card>
  <Card title="Use Project Names Consistently" icon="setting">
    When saving memories, use consistent project names so related memories can be recalled together.
  </Card>
  <Card title="Check Available Libraries" icon="magnifier">
    Use the Explore page to see which libraries are indexed. Not all libraries may be available.
  </Card>
</CardGrid>

## Next Steps

- Learn about all [MCP tools](/mcp-tools/) available
- Explore the [REST API](/api/) for programmatic access
- [Submit a library](/submit) for indexing
- [Browse indexed libraries](https://nexus.yogan.dev/explore) to see what's available
