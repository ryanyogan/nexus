{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler types && wrangler dev --persist-to ../../.wrangler/state",
    "build": "wrangler types && wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler types && wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@nexus/auth": "workspace:*",
    "@nexus/db": "workspace:*",
    "@nexus/logger": "workspace:*",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.7.11",
    "zod": "^3.25.34"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.40.3"
  }
}
